const addTm = document.querySelector(".add-tm")
const tmBar = document.querySelector("#tm-bar")
const tmDirectory = document.querySelector('#tm-directory')

addTm.addEventListener('click', (e) => {
    showForm()
});